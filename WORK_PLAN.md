# 旅遊 / 出差行程規劃器（7 天）— 工作計畫 & SOP 勾選清單

目標：完成一個前端單頁網站，能新增 / 編輯 / 刪除行程，支援「日期 + 時間」選擇並自動排序；可輸入行程名稱、地點、交通方式、備註；備註欄支援輸入網址並可選取套用為超連結；預設以 7 天行程視圖呈現；介面風格調整為「少女可愛風」，並支援手機優先查看看板。

交付物（檔案切分）：
- `index.html`
- `style.css`
- `app.js`

---

## 工作流程（SOP）

目前進度摘要：
- 已完成網站切分：`index.html`、`style.css`、`app.js`
- 已完成：資料保存、自動排序、7 天看板、匯入/匯出、手機板收合表單、多旅程管理、預算統計、Google Maps 地圖連結
- 已修正：`localStorage` 儲存錯誤處理、手機板面板節點不一致、備註連結儲存清理邏輯
- 登入系統進度：已加入 `login.html`、Supabase 設定說明、主頁登入檢查與登出按鈕，但仍需除錯與驗收
- 待確認：跨裝置實機驗收、更多情境測試、Supabase 登入流程實測

### 0) 開工前（需求鎖定）
- [x] 確認行程欄位：日期、時間、標題、地點、交通方式、備註（可含連結）
- [x] 確認排序規則：依「日期時間」升冪排序（同時間再依建立順序）
- [x] 確認 7 天範圍：以使用者選定「起始日」起算連續 7 天（含當天）
- [x] 確認資料保存：使用 `localStorage`（可離線、重整不丟失）
- [x] 確認編輯行為：可點選行程進入編輯、保存、取消

### 1) 資料模型（Data Model）
- [x] 定義 `TripItem` 結構（id、startLocal、title、location、transport、notesHtml、createdAt）
- [x] 實作序列化 / 反序列化（localStorage）
- [x] 實作排序方法（以日期時間排序）
- [x] 實作 7 天視圖用的日期區間產生器（startDate -> 7 days）

### 2) UI/UX（少女可愛風版面）
- [x] 規劃版面：桌機雙欄 / 手機單欄 + 7 天行程看板
- [x] 設計「少女可愛風」視覺語彙：粉嫩色系、圓角、膠囊按鈕、柔和背景圖樣
- [x] 互動回饋：hover、focus、active、錯誤提示（必填/格式）
- [x] 無障礙基本：可鍵盤操作、可見焦點、label 對應 input

### 3) 表單功能（新增 / 編輯）
- [x] 日期輸入（date）
- [x] 時間輸入（time）
- [x] 行程名稱（text）
- [x] 地點（text，可自由輸入）
- [x] 交通方式（select + 自訂選項或文字輸入）
- [x] 備註（可編輯富文字區塊）
- [x] 備註連結：提供「選取文字 → 套用超連結」的操作
- [x] 表單驗證：日期/時間必填；URL 格式檢查（http/https）

### 4) 行程列表 / 看板（自動排序）
- [x] 顯示 7 天欄位（每一天一欄）
- [x] 依日期分組，組內依時間排序
- [x] 行程卡片：顯示時間、標題、地點、交通、備註（可點連結）
- [x] 卡片操作：編輯、刪除（含二次確認）

### 5) 儲存與狀態
- [ ] 初次進站：若無資料，建立示範資料（目前未提供）
- [x] 任一變更即保存 localStorage
- [x] 提供匯出 / 匯入（JSON）
- [x] 提供清空全部
- [x] 補上 `localStorage` 失敗時的錯誤提示與回復流程

### 5.5) 旅程管理 / 統計 / 地圖
- [x] 提供多旅程切換、新增、重新命名、刪除
- [x] 每個旅程獨立保存起始日與行程列表
- [x] 提供預算欄位與總預算 / 平均預算統計
- [x] 地點可直接開啟 Google Maps 搜尋
- [x] 舊版單旅程資料可升級到多旅程格式

### 5.6) 登入 / 權限（Supabase）
- [x] 新增 `login.html` 登入頁
- [x] 新增 `SUPABASE_SETUP.md` 設定說明
- [x] 主頁加入登入檢查與登出流程
- [x] 登入後查詢 `profiles.role`
- [ ] 修正 Supabase 初始化與主頁整合的阻塞問題
- [ ] 完成未登入跳轉、登入成功跳轉、登出返回的實機驗收
- [ ] 補上註冊流程或建立測試帳號流程
- [ ] 改善憑證管理方式，避免直接寫死在前端檔案

### 6) 測試 & 驗收
- [ ] 新增 10 筆跨日行程後，排序正確
- [ ] 同日同時多筆行程顯示穩定（順序可預期）
- [ ] 重整頁面資料仍在
- [ ] 備註連結可點擊，且不影響其他文字顯示
- [ ] 手機/窄螢幕可用（已完成版型，待實機驗收）
- [ ] 多旅程切換後資料正確隔離
- [ ] 預算統計與卡片預算顯示正確
- [ ] Google Maps 連結可正確帶入地點
- [ ] Supabase 登入 / 登出 / 角色驗證流程可正常執行

---

## 里程碑（建議）
- [x] M1：完成資料模型 + localStorage + 排序（無 UI 也可運作）
- [x] M2：完成表單新增/編輯 + 基本列表
- [x] M3：完成 7 天看板 + 分組顯示
- [x] M4：套用少女可愛風視覺 + 手機收合互動
- [x] M4.5：完成多旅程管理 + 預算統計 + 地圖連結
- [ ] M5：驗收清單全數通過
- [ ] M6：完成登入 / 權限驗收並接上可用的帳號流程

---

## 風險/注意事項
- [x] 時區：以使用者本地時間為準（存檔為本地日期時間字串，顯示也用本地）
- [x] URL 安全：只允許 `http://` 或 `https://`；渲染連結時加上 `rel="noopener noreferrer"`
- [x] 可維護性：樣式集中於 `style.css`，互動集中於 `app.js`
- [ ] Supabase 憑證目前直接寫在前端檔案，正式上線前需重新檢視
- [ ] 登入與主頁目前都有直接依賴 Supabase CDN 與 `profiles` 表結構，需做實機驗證
- [ ] 建議下一步：補上實機驗收紀錄與操作截圖
